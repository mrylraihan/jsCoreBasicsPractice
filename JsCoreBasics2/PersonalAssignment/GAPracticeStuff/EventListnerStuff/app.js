const btn = document.querySelector('button')





// we need locate the ul tag 
const ul = document.querySelector('ul')
// grab the lis from the ul 
const listLi = ul.querySelectorAll('li')
//index the right li
console.log(listLi[4].innerText);
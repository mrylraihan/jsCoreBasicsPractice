const fName = 'Wallie'
    const lName = 'Raihan'

    const fullName = (a = 'John', b ='Doe') => `hey im ${a} ${b}`

const firstSentence = fullName(fName, lName)
const firstSentencetest = fullName()
console.log(firstSentencetest);

const secondSentence = greet()

        function greet() {
            return "it’s nice to meet you"
        }
        console.log(firstSentence)

        console.log(secondSentence)
